# CFS3_TEACHER_SPEC — 教師の合格条件 v1.0

制定: 2026-07-02（ARK演繹・ヨーク承認待ち）
目的: 「どういう10x解なら本物と認めるか」を探索の**前**に固定する。
探索の手段は無制限（未来情報・神の目・ML可）。このゲートだけが不動。
数値の変更は可能だが、必ずヨーク承認＋LEDGERに理由を記録してから（探索の都合による後出し緩和の禁止）。

---

## 0. 教師の定義

教師 = **選択ルール R** と、Rが54ヶ月データ上に生成する**実現トレード集合**の組。

- **Rの内部は執行時点情報のみ**: Rが毎日の候補を決めるのに使ってよいのは、その時点までに観測可能な情報だけ（価格・出来高・需給・財務発表済み値・地合い等）。未来参照フィールドはゼロ（T6-aで機械検査）
- **未来情報はRの探索にのみ使用可**: 「どのRが10xを出すか」を見つける過程では神の目・未来netを自由に使ってよい。R本体に混ぜることだけが禁止
- 執行はCORE準拠: entry=AdjO[t+1]、コスト・税込みnet、ギャップは寄値約定、資金配分は複利（現金内、レバなし）

---

## 1. 合格条件（T1〜T6、全て同時に満たす。scriptがassertする）

### T1. 10x方程式
- ローリング16ヶ月窓（327営業日、開始を1ヶ月刻みでずらした全窓≒39窓）で複利multを測る
- **中央値 ≥ 10.0** かつ **下位10%窓 ≥ 5.0** かつ **最悪窓 ≥ 1.5**
- multは全て実行コスト・税込みのnet確定畳み複利

### T2. 一点解でない（針の排除）
- Rの全数値パラメータを個別に **±20%**（分位パラメータは±1刻み）摂動
- 全摂動で **中央値mult ≥ 5.0** を維持。半減以上の崩壊が1箇所でもあれば不合格

### T3. 期間・地合い非依存
- 54ヶ月を前半/後半に二分し、各半分単独で **T1の中央値 ≥ 7.0**
- どの単一年（暦年）を除去しても全期間中央値 ≥ 8.0（単年依存の排除）

### T4. 一発依存でない
- 各16ヶ月窓で、log利益寄与上位5%のトレードを除去しても **mult ≥ 3.0**
- どの単一トレードを除去しても窓mult ≥ 8.0
- （注: 大化けを**含む**ことは禁止しない。特定の少数トレードに**依存する**ことを禁止する）

### T5. 執行可能性
- 各エントリー日: 配分額で最低単元（100株）が買える（`alloc ≥ price×100`）
- 各エントリー: 約定金額 ≤ 当日売買代金Vaの **1%**
- 常時 現金 ≥ 0（レバなし）、K分散・保有ロックの物理整合
- universe: 上場60日以上 / 価格100〜50,000円 / blacklist除外

### T6. 構造性（情報ゼロ教師の排除）
- **T6-a 未来参照ゼロ**: Rの参照フィールドをホワイトリストと機械照合。未来由来フィールドが1つでもあれば不合格
- **T6-b 交換可能性**: 候補数がスロット数を超える日の選択をランダム化し、1000シードで再実行。**中央値 ≥ 10.0、下位5% ≥ 7.0**
  （= 10xが「個体を当てた」結果でなく「状態に居た」結果であることの検査。個体識別依存の教師はここで死ぬ）

---

## 2. 判定の運用

- 合否宣言はscriptのassert出力のみが有効。ARKの言葉による合否宣言は無効
- 検証scriptは実行前にどのT条件を検査するかをheaderに宣言し、結果はLEDGERに機械追記
- 部分合格（例: T1-T5合格・T6のみ不合格）は「不合格・ただしT6のみ残」として事実記録する

## 3. 探索側への注記

- 合格ラインは意図的に苛烈にしてある。当たり前のアプローチ（既知の因子・常識的な選別）はT1すら通らないことが10ヶ月分のデータで分かっている。**ゲートを緩めるのではなく、手段を型破りにする**のがCFS3の解き方
- 「条件を満たす解が存在するか」自体が最初の実証テーマ。全滅もデータであり、その場合は不足量（どのTで、どれだけ足りないか）を数値で記録する

---

## 改訂履歴
- 2026-07-02 v1.0 初版（6条件を数値化。全数値はv1であり、変更はヨーク承認＋LEDGER記録で行う）
