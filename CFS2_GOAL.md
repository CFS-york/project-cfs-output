# CFS2 GOAL — 16か月10xの定義とアプローチ（計算式）

## ★★★ CFS2 五大原則（毎回最初に読む。全条項に優先）★★★
```
1. 16か月10x       : 30万 → 300万 を 16か月(約344営業日)で。
2. 現実約定可能     : 実トレードで執行できること(バックテスト固有の非現実な約定を排除)。
3. レバ禁止・複利全開: レバレッジ不使用。利益は全額再投資(フル複利)。
4. 銘柄を当てない   : 個別銘柄を「これが上がる」と当てに行く手法でない。再現可能な規則で回す。
5. 大化け依存禁止   : 大化けを狙って賭けない。副産物で拾うのは可だが、極端な大化けを除いた数値で
                     手法が成立していること。10xは再現可能な小エッジの複利で作る。
```
★この五大原則は、仮説設計・検証評価の全てに優先する。毎セッション、これを最初に確認する。



```
場所: C:\mnt\data\cfs2\CFS2_GOAL.md
位置づけ: cfs2の土台。全ての世界はこの定義に照らして設計・判定される。
　　　　　世界(答え)を出す前に、この定義(問い)を持つ。AUDITOR検証の基準もこれ。
ヨーク指摘(2026-06-26): 「16か月10xの定義とアプローチを示さないと、何をAUDITORにぶつけるのか」
```

## 1. 定義（何をもって10x達成か）
- 初期資産 30万円 → 最終資産 300万円
- 期間: 16か月 = 約344営業日
- レバなし、複利、現実約定可能
- **10x条件**: 資産 = 30万 × Π(1+r_i) ≥ 300万 ⇔ **Π(1+r_i) ≥ 10**
- 対数化: **Σ ln(1+r_i) ≥ ln(10) = 2.303**
  - r_i = i番目トレードの純リターン（コスト・税引後）

## 2. アプローチ（10xへ至るロジックと計算式）

### 中核式
- N = 総トレード回数（複利の直列回数）
- m = 平均対数リターン = 平均[ln(1+r_i)]
- **10x条件: N × m ≥ 2.303**

### m の分解（勝率とリターン構造）
```
m = p·ln(1+w) + (1−p)·ln(1+l)
  p = 勝率
  w = 平均勝ちリターン（純、>0）
  l = 平均負けリターン（純、<0）
```

### 純リターン（コスト・税の計算）
```
r_gross = 総リターン（価格変化のみ）
r_net   = r_gross − 2·COST − (r_gross>0 なら 利益分×TAX)
  COST = 0.005（片道、往復2倍）
  TAX  = 0.20315（利益への課税）
```

### N（回数）と保有・並列の関係
```
H = 平均保有日数
K = 同時保有数（並列）
直列近似: N ≈ (344 / H) × 実効並列度
★並列(K>1)は資金を分割するので、複利の直列性を弱める。
　1銘柄集中(K=1)なら純粋な直列複利。分散は平均回帰でmを下げるがリスクを下げる。
```

## 3. 10xの筋（代表例、この式から逆算）
| 保有H | 最大N(K=1) | 必要m | 必要平均r(近似) | 必要な勝ち方の例 |
|---|---|---|---|---|
| 20日 | 17 | 0.135 | +14.5% | p=0.4, w=+50%, l=−8% → m≈0.14 |
| 40日 | 8 | 0.288 | +33% | p=0.4, w=+120%, l=−12% 右裾依存 |
| 60日 | 6 | 0.384 | +47% | p=0.35, w=+180%, l=−15% 強右裾 |
| 5日 | 68 | 0.034 | +3.5% | p=0.55, w=+12%, l=−7% 高頻度高勝率 |

★どの筋も、勝ちリターンwの右裾が効く。高頻度(H小)はNが稼げるがコストが重い。低頻度(H大)はコストは軽いが1回の右裾が要る。

## 4. AUDITOR検証の基準（何をぶつけるか）
各世界について、実データで以下を測る:
1. **N**: 16か月でその条件が何回発火したか
2. **p, w, l**: 発火した各トレードの勝率・平均勝ち・平均負け（純リターン）
3. **m = p·ln(1+w)+(1−p)·ln(1+l)** を計算
4. **N×m ≥ 2.303 か** = 10x到達判定
5. 実際の複利sim（Π(1+r_i)）で最終倍率を出し、上の理論と照合

→ AUDITORは「世界の条件通りにトレードした時の (N,p,w,l) を実データで測り、10xの筋に乗るか」を判定する。これが検証の中身。

## 5. 世界設計の規律（この定義に照らす）
- 全世界は「どの筋（表3のどれか、または新しい筋）で10xに至るか」を、(N,p,w,l)の目標値で書く。
- 「年40回×+6%」のような曖昧な一行でなく、「N=40, p=0.45, w=+18%, l=−9% → m=0.043, N×m=1.72」のように、式に乗る形で。
- AUDITOR検証で実測(N,p,w,l)が目標に届かなければ、その世界は10x未達。

★この文書がcfs2の土台。世界(WORLDS)は、この定義とアプローチに照らして設計・判定される。
